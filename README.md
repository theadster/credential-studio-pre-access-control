# CredentialStudio

This is a comprehensive event credential management application that includes:

1. Next.js with Pages Router
2. Tailwind CSS Framework
3. Context for global state management
4. Appwrite for backend services

## Features

- **Event Credential Management**: Complete CRUD operations for attendee records with custom fields and photo uploads
- **User Authentication**: Secure authentication system with role-based access control using Appwrite Auth
- **Role Management**: Granular permissions and role management for different user types
- **Event Configuration**: Comprehensive event settings including barcode generation and custom fields
- **Photo Integration**: Seamless photo uploads with Cloudinary widget integration
- **Professional Printing**: High-quality credential printing with Switchboard Canvas API
- **Activity Logging**: Full audit trail of all user actions and system changes
- **Beautiful UI**: Modern, responsive design with Framer Motion animations and shadcn/ui components
- **Database Integration**: Appwrite Database for all data storage and real-time updates

## Prerequisites

- Node.js 20.x or higher
- An Appwrite account (cloud or self-hosted)
- Cloudinary account for image uploads
- Switchboard Canvas account for credential printing

## Getting Started

### 1. Clone and Install

```bash
git clone <repository-url>
cd credentialstudio
npm install
```

### 2. Set Up Appwrite Project

1. Create a new project in [Appwrite Cloud](https://cloud.appwrite.io) or your self-hosted instance
2. Note your Project ID from the project settings
3. Create an API key with the following scopes:
   - `users.read`, `users.write`
   - `databases.read`, `databases.write`
   - `collections.read`, `collections.write`
   - `documents.read`, `documents.write`

### 3. Configure Database

Run the Appwrite setup script to create all required collections:

```bash
npm run setup:appwrite
```

This script will:
- Create the database
- Create all required collections (users, roles, attendees, custom_fields, event_settings, logs, log_settings, invitations)
- Configure attributes and indexes
- Set up collection permissions

Alternatively, see `APPWRITE_SETUP.md` for manual setup instructions.

### 4. Configure OAuth Providers (Optional)

To enable Google OAuth:

1. Go to your Appwrite project → Auth → Settings
2. Enable Google OAuth provider
3. Add your Google OAuth credentials
4. Add authorized redirect URLs:
   - `http://localhost:3000/auth/callback` (development)
   - `https://yourdomain.com/auth/callback` (production)

### 5. Environment Variables

Create a `.env.local` file in the root directory:

```env
# Appwrite Configuration
NEXT_PUBLIC_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
NEXT_PUBLIC_APPWRITE_PROJECT_ID=your_project_id
APPWRITE_API_KEY=your_api_key

# Appwrite Database IDs (generated by setup script)
NEXT_PUBLIC_APPWRITE_DATABASE_ID=your_database_id
NEXT_PUBLIC_APPWRITE_USERS_COLLECTION_ID=users_collection_id
NEXT_PUBLIC_APPWRITE_ROLES_COLLECTION_ID=roles_collection_id
NEXT_PUBLIC_APPWRITE_ATTENDEES_COLLECTION_ID=attendees_collection_id
NEXT_PUBLIC_APPWRITE_CUSTOM_FIELDS_COLLECTION_ID=custom_fields_collection_id
NEXT_PUBLIC_APPWRITE_EVENT_SETTINGS_COLLECTION_ID=event_settings_collection_id
NEXT_PUBLIC_APPWRITE_LOGS_COLLECTION_ID=logs_collection_id
NEXT_PUBLIC_APPWRITE_LOG_SETTINGS_COLLECTION_ID=log_settings_collection_id
NEXT_PUBLIC_APPWRITE_INVITATIONS_COLLECTION_ID=invitations_collection_id

# Team Membership (Optional - for auth user linking)
NEXT_PUBLIC_APPWRITE_PROJECT_TEAM_ID=your_team_id
APPWRITE_TEAM_MEMBERSHIP_ENABLED=true

# Rate Limiting (Optional - for email verification)
VERIFICATION_EMAIL_USER_LIMIT=3
VERIFICATION_EMAIL_ADMIN_LIMIT=20
VERIFICATION_EMAIL_WINDOW_HOURS=1

# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Switchboard Canvas Configuration
NEXT_PUBLIC_SWITCHBOARD_API_KEY=your_switchboard_key
```

#### Environment Variable Details

**Required Variables:**
- `NEXT_PUBLIC_APPWRITE_ENDPOINT` - Your Appwrite API endpoint
- `NEXT_PUBLIC_APPWRITE_PROJECT_ID` - Your Appwrite project ID
- `APPWRITE_API_KEY` - Server-side API key with appropriate scopes
- All `NEXT_PUBLIC_APPWRITE_*_COLLECTION_ID` variables - Generated by setup script

**Optional Variables:**
- `NEXT_PUBLIC_APPWRITE_PROJECT_TEAM_ID` - Team ID for automatic team membership when linking users
- `APPWRITE_TEAM_MEMBERSHIP_ENABLED` - Enable/disable team membership feature (default: false)
- `VERIFICATION_EMAIL_USER_LIMIT` - Max verification emails per user per hour (default: 3)
- `VERIFICATION_EMAIL_ADMIN_LIMIT` - Max verification emails per admin per hour (default: 20)
- `VERIFICATION_EMAIL_WINDOW_HOURS` - Rate limit window in hours (default: 1)

### 6. Initialize Default Roles

Run the role initialization endpoint to create default roles:

```bash
curl -X POST http://localhost:3000/api/roles/initialize
```

Or visit the endpoint in your browser after starting the development server.

### 7. Run Development Server

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) in your browser to see the application.

## Project Structure

```
├── src/
│   ├── pages/              # Next.js pages and API routes
│   │   ├── api/           # API endpoints
│   │   ├── auth/          # Authentication pages
│   │   └── *.tsx          # Application pages
│   ├── components/        # Reusable React components
│   │   └── ui/           # shadcn/ui component library
│   ├── contexts/          # React Context providers (AuthContext)
│   ├── hooks/             # Custom React hooks
│   ├── lib/               # Utility libraries and configurations
│   │   ├── appwrite.ts   # Appwrite client setup
│   │   └── appwriteQueries.ts  # Query helpers
│   └── styles/            # Global styles
├── scripts/               # Setup and utility scripts
├── public/                # Static assets
└── .kiro/                 # Kiro configuration
```

## Development Workflow

### Common Commands

```bash
npm run dev          # Start development server
npm run build        # Build for production
npm run start        # Start production server
npm run lint         # Run ESLint
```

### Working with Appwrite

- **Appwrite Console**: Access your project at https://cloud.appwrite.io
- **Real-time Updates**: The application uses Appwrite Realtime for live data updates
- **Authentication**: Managed through Appwrite Auth with support for email/password, OAuth, and magic links
- **Database**: All data stored in Appwrite Database collections with automatic timestamps

### Data Migration

If migrating from an existing Supabase instance, see `MIGRATION_QUICK_START.md` for instructions on using the migration script.

## Technology Stack

- **Frontend**: Next.js 15.5.2, React 19.1.1, TypeScript
- **Styling**: Tailwind CSS, shadcn/ui, Framer Motion
- **Backend**: Appwrite (Auth, Database, Realtime)
- **Image Management**: Cloudinary
- **Printing**: Switchboard Canvas API

## Learn More

To learn more about the technologies used in this application:

- [Next.js Documentation](https://nextjs.org/docs)
- [Appwrite Documentation](https://appwrite.io/docs)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- [shadcn/ui Documentation](https://ui.shadcn.com)
- [React Context API](https://reactjs.org/docs/context.html)

## Support

For issues or questions:
1. Check the documentation files in the root directory
2. Review the Appwrite setup guide in `APPWRITE_SETUP.md`
3. Consult the migration documentation if upgrading from Supabase

## License

[Your License Here]
