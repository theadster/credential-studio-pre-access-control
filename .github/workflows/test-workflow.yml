name: Test Workflow

on:
  push:
    branches:
      - feature/mobile-access-control

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test step
        run: echo "✅ Workflow triggered successfully"
